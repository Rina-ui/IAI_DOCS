export declare class Performance {
    readonly id: string;
    studentId: string;
    subject: string;
    score: number;
    rank: number;
    constructor(id: string, studentId: string, subject: string, score: number, rank?: number);
}
